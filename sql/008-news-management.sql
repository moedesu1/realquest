-- ============================================================
-- REALQUEST ニュース管理マニュアル
-- ============================================================
--
-- 【操作場所】
--   1. https://supabase.com にログイン
--   2. REALQUESTのプロジェクトを開く
--   3. 左メニューの「SQL Editor」をクリック
--   4. 下のSQL文をコピペして「Run」ボタンを押す
--
-- ============================================================


-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ■ ニュースを追加する
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ↓ この4箇所を書き換えるだけ！

INSERT INTO public.news (title, body, category, published_at) VALUES (
  'ここにタイトルを入力',
  'ここに本文を入力',
  'info',
  now()
);

-- category の選択肢:
--   'info'      → 「お知らせ」として表示
--   'new'       → 「新着」として表示
--   'important' → 「重要」として表示
--
-- published_at:
--   now()           → 今すぐ公開
--   '2026-04-01'    → 4月1日に表示開始（日付指定）


-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ■ 今あるニュース一覧を確認する
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

-- SELECT id, title, category, published_at FROM public.news ORDER BY published_at DESC;


-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ■ ニュースを修正する
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- まず上の一覧確認で id を調べてから ↓

-- UPDATE public.news
-- SET title = '新しいタイトル', body = '新しい本文'
-- WHERE id = 1;
--                  ↑ ここに修正したいニュースの id を入れる


-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ■ ニュースを削除する
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

-- DELETE FROM public.news WHERE id = 1;
--                                  ↑ 削除したいニュースの id


-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ■ もっと簡単に管理したい場合
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- Supabaseダッシュボードの「Table Editor」からも操作できます：
--   1. 左メニュー「Table Editor」をクリック
--   2. テーブル一覧から「news」を選択
--   3. 「Insert row」ボタンで新しい行を追加
--   4. 各カラムに値を入力して「Save」
--
-- ※ SQL Editorの方が慣れると早いですが、
--   Table Editorはエクセルみたいに使えるので初心者向けです。
